import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    fontSize: 16,
    textAlign: 'center',
    flexShrink: 1,
    marginHorizontal: 16,
    alignSelf: 'flex-start',
  },
});
