import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    fontSize: 10,
    textDecorationLine: 'line-through',
  },
  unit: {
    fontSize: 10,
    flexShrink: 1,
    opacity: 0,
    marginLeft: 4,
  },
});
