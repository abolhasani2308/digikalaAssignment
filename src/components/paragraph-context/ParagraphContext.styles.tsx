import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    fontSize: 14,
    marginHorizontal: 16,
    marginTop: 12,
    lineHeight: 32,
    textAlign: 'right',
    marginBottom: 24,
  },
});
